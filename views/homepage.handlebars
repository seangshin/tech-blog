<div class="container">
  {{#each posts as |post| }}
  <div class="row p-3">
    <h1>
      {{post.name}}
    </h1>
    <p>
      {{post.description}}
    </p>
   <p>
      Posted by {{post.user.name}}
    </p>
  </div>
  {{/each}}
</div>
